%div
	= beverage.name
	= beverage.description
	= number_to_currency(beverage.price)
	= beverage.available
	= link_to 'Edit', edit_beverage_path(beverage)
	= link_to 'Delete', beverage_path(beverage), :method => :delete