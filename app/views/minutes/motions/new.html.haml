%h1 Neuer Antrag

%p Im Protokoll vom <strong>#{l @minutes_minute.date}</strong>, zum Tagesordnungspunkt <strong>#{@minutes_item.title}</strong>.

= render 'form'

= link_to t('actions.back'), @minutes_minute
