<div class="actions">
	<h1>Eintrag bearbeiten</h1>
	<%= link_to content_tag(:span, ' Abbrechen', :class => "icon-cancel-1"), user_path(@user), :class => "button small round secondary" %>
</div>

<%= render 'form' %>