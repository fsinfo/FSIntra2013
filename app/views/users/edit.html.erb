<div class="actions">
	<h1>Eintrag bearbeiten</h1>
	<a href="<%= user_path(@user) %>" class="button small round secondary">
		<span class="icon-cancel-1"> Abbrechen</span>
	</a>
</div>

<%= render 'form' %>